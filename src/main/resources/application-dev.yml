server:
  port: 8083
#database connection
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app
    username: root
    password: aryan@2006

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create  # Changed from update to force table creation logs
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        # This line ensures the DDL (Create statements) actually shows up in logs
        use_sql_comments: true